//
//  Exercise_Adder.swift
//  Workout-Diary
//
//  Created by arpit on 08/07/26.
//

import SwiftUI

struct Exercise_Adder: View {
    
   // private var ex_name:String = Exercise_Chooser.ex_name
    var body: some View {
        VStack{
          /*  Text("Exercise :  " + ex_name)
                .font(Font.system(size: 25))
                .padding()
                .background(Color.gray, in: RoundedRectangle(cornerRadius: 25))
            Button("Add Set", systemImage: "plus.app.fill") {
                //Exercise_Adder()
            }
            .font(Font.system(size: 25))
            .buttonStyle(.borderedProminent)
            .tint(.indigo) // Changes the background color of prominent buttons
            .controlSize(.large) */
            
            
            
            
            
            
        }
        //.background(Color.gray, in: RoundedRectangle(cornerRadius: 25))

    }
}

#Preview {
    Exercise_Adder()
        .preferredColorScheme(.dark)
}
